import baseFontFamilies from './baseFontFamilies';

const fontFamilies = {
  ...baseFontFamilies,
  base: baseFontFamilies.inter,
  heading: baseFontFamilies.poppins,
  monospace: baseFontFamilies.robotoMono,
};

export default fontFamilies;
